# CPU-only PyTorch (much smaller than GPU version)
# IMPORTANT: Railway needs CPU-only version to stay under 4GB limit
# For local dev, you can use regular torch, but Railway MUST use CPU-only
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.0.1+cpu
torchvision==0.15.2+cpu

# Core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
# Use headless OpenCV (no GUI dependencies)
opencv-python-headless==4.8.1.78
# numpy 1.26.x is compatible with pandas 2.3.3 (required by ultralytics)
# numpy 1.24.3 causes "Cannot convert numpy.ndarray to numpy.ndarray" error with pandas 2.3.3
numpy==1.26.4
Pillow==10.1.0
# Install ultralytics but tell it to use opencv-python-headless
ultralytics==8.1.0
# HTTP client for Grok API
requests==2.31.0
# Optional: ByteTrack for tracking (can use simple overlap tracking instead)
# byte-track==1.0.0
